# Claude Skills Pack

Bộ commands, agents và skills cho Claude Code.

## Cài đặt

```bash
git clone https://github.com/yeuvjaj252/claude-skills-pack.git
cd claude-skills-pack
./install.sh
```

## Gỡ cài đặt

```bash
./uninstall.sh
```

## Nội dung

| Thành phần | Số lượng |
|------------|----------|
| Commands   | 20 files |
| Agents     | 16 files |
| Skills     | 42 folders |

## Commands có sẵn

`/backend`, `/brainstorm`, `/create`, `/database`, `/debug`, `/deploy`, `/devops`, `/docs`, `/enhance`, `/explore`, `/frontend`, `/mobile`, `/orchestrate`, `/performance`, `/plan`, `/preview`, `/security`, `/status`, `/test`, `/ui`
